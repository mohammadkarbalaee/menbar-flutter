import 'package:flutter/cupertino.dart';

class Orators extends StatelessWidget {
  const Orators({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
          'Orators'
      ),
    );
  }
}
