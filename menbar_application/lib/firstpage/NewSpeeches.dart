import 'package:flutter/cupertino.dart';

class NewSpeeches extends StatelessWidget {
  const NewSpeeches({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
          'New speeches'
      ),
    );
  }
}
